/**
 * ECardState determines which column the card should reside in 
 */
enum ECardState {
    todo = 'todo',
    doing = 'doing',
    done = 'done',
    regression = 'regression'
}
export default ECardState;