/**
 * ECard type is the type of the card
 *? Will decide if the card looks like a 'task' card or a 'regression' card for now 
 */
enum ECardType {
    task = 'task',
    regression = 'regression'
}
export default ECardType;